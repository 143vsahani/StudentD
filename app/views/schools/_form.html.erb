<%= form_with(model: @school, local: true) do |form| %>
  <% if @school.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@school.errors.count, "error") %> prohibited this school from being saved:</h2>

      <ul>
        <% @school.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :board %>
    <%= form.text_field :board %>
  </div>

  <div class="field">
    <%= form.label :schoolName %>
    <%= form.text_field :schoolName %>
  </div>

  <div class="field">
    <%= form.label :principalName %>
    <%= form.text_field :principalName %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
