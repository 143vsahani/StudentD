<!-- app/views/students/index.html.erb -->
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Teacher Dashboard</title>
  <%= stylesheet_link_tag 'https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css', media: 'all' %>
  <style>
    body {
      font-family: 'Arial', sans-serif;
      background-color: #f4f4f4;
      margin: 0;
      padding: 0;
    }

    .container {
      max-width: 800px;
      margin: 50px auto;
      background-color: #fff;
      padding: 20px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }

    h1 {
      color: #333;
      text-align: center;
    }

    .table {
      width: 100%;
      margin-top: 20px;
      border-collapse: collapse;
      margin-bottom: 20px;
    }

    th, td {
      padding: 10px;
      border: 1px solid #ddd;
      text-align: left;
    }

    th {
      background-color: #007bff;
      color: #fff;
    }

    td {
      background-color: #f9f9f9;
    }

    .actions {
      display: flex;
      justify-content: space-around;
      align-items: center;
    }

    .btn {
      text-decoration: none;
      padding: 5px 10px;
      border-radius: 5px;
    }

    .btn-info {
      color: #fff;
      background-color: #007bff;
      border: 1px solid #007bff;
      transition: background-color 0.3s ease;
    }

    .btn-info:hover {
      background-color: #0056b3;
    }

    .btn-warning {
      color: #212529;
      background-color: #ffc107;
      border: 1px solid #ffc107;
      transition: background-color 0.3s ease;
    }

    .btn-warning:hover {
      background-color: #d39e00;
    }

    .btn-danger {
      color: #fff;
      background-color: #dc3545;
      border: 1px solid #dc3545;
      transition: background-color 0.3s ease;
    }

    .btn-danger:hover {
      background-color: #c82333;
    }

    .links {
      display: flex;
      justify-content: space-around;
    }

    .links a {
      text-decoration: none;
      color: #fff;
      padding: 10px 20px;
      border-radius: 5px;
      transition: background-color 0.3s ease;
    }

    .links .btn-primary {
      background-color: #007bff;
      border: 1px solid #007bff;
    }

    .links .btn-primary:hover {
      background-color: #0056b3;
    }
  </style>
</head>
<body>

<div class="container">
  <h1 class="mb-4">Teacher Dashboard</h1>

  <table class="table">
    <thead class="thead-dark">
      <tr>
        <th>Name</th>
        <th>Email</th>
        <th>Contact Number</th>
        <th>Actions</th>
      </tr>
    </thead>

    <tbody>
      <% @teachers.each do |teacher| %>
        <tr>
          <td><%= teacher.name %></td>
          <td><%= teacher.email %></td>
          <td><%= teacher.contactNumber %></td>
          <td class="actions">
            <%= link_to 'Show', teacher, class: 'btn btn-info' %>
            <%= link_to 'Edit', edit_teacher_path(teacher), class: 'btn btn-warning' %>
            <%= link_to 'Destroy', teacher, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger' %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>

 <div class="links">
  <%= link_to 'Show Class', school_class_teachers_path, class: 'btn btn-primary' %>
  <%= link_to 'Show School', school_teachers_path, class: 'btn btn-primary' %>
  <%= link_to 'Show Student', show_student_teachers_path, class: 'btn btn-primary' %>
</div>


</body>
</html>
